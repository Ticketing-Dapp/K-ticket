import 'package:flutter/material.dart';

class MyLike extends StatelessWidget {
  static const String id = 'my_like';

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('My Like Concert'),
      ),
    );
  }
}
